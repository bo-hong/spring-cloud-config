eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: order
  rabbitmq:
    host: 192.168.1.8
    port: 5672
    username: user
    password: 123
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: pro
server:
  port: 8081
